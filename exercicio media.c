#include <stdio.h>
#include <stdlib.h>

int main (void){
	

	int quantidademinima, quantidademaxima, media;
printf("digite a quantidade minima:\n")	;
scanf("%d",&quantidademinima);

printf ("digite quantidade maxima:\n");
scanf ("%d",&quantidademaxima);

 media = (quantidademaxima + quantidademinima)/2;
 printf ("a media e %d \n", media);

if (estoquemedio,<=100)
printf("o estoque precisa ser abastecido");
else

system("pause");
return 0;
}
